title: About Page

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          subtitle:
            type: text
          text:
            type: textarea
            size: huge
          
  sidebar:
    width: 1/3
    sections:
      photo:
        headline: Photostrip
        size: small
        info: "{{ file.dimensions }}"
        help: 600 tall by whatever.
        type: files
        multiple: true
        layout: cards
        image:
          ratio: 1/1

